<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tubes.hibanyumas">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".komentar.ui.InputQuotesActivity"></activity>
        <activity android:name=".komentar.ui.KomentarDetailActivity" />
        <activity
            android:name=".komentar.KomentarActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".tempatbersejarah.WisataBersejarahDetailActivity"
            android:label="@string/title_activity_hotel_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".wisataalam.WisataAlamDetailActivity"
            android:label="@string/title_activity_hotel_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".kuliner.KulinerDetailActivity"
            android:label="@string/title_activity_hotel_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".hotel.HotelMapsActivity"
            android:label="@string/title_activity_hotel_maps" />
        <activity
            android:name=".hotel.HotelDetailActivity"
            android:label="@string/title_activity_hotel_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".kuliner.RecycleView5"
            android:label="@string/tugas5" />
        <activity
            android:name=".tempatbersejarah.RecycleView4"
            android:label="@string/tugas4" />
        <activity
            android:name=".wisataalam.RecycleView3"
            android:label="@string/tugas3" />
        <activity
            android:name=".oleholeh.RecycleView2"
            android:label="@string/tugas2" />
        <activity
            android:name=".hotel.RecycleView"
            android:label="@string/tugas1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>